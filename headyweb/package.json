{
  "name": "headyweb",
  "version": "1.0.0",
  "description": "HeadyWeb — AI-powered browser shell with HeadyBuddy companion sidebar",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js --dev",
    "build": "echo 'HeadyWeb is a static + API app — no build step required'",
    "worker:dev": "npx wrangler dev worker.js",
    "worker:deploy": "npx wrangler deploy worker.js"
  },
  "dependencies": {
    "express": "^4.18.0",
    "cors": "^2.8.5",
    "compression": "^1.7.4"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "author": "Heady Systems",
  "license": "MIT"
}
